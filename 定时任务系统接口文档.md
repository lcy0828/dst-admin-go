# 定时任务系统接口详细说明

本文档详细描述了定时任务系统的各个接口，包括基本的任务管理接口和tmux集成接口。

## 一、基本任务管理接口

### 1. 获取所有任务

**接口路径**：`GET /api/cron/tasks`

**功能描述**：获取系统中所有的定时任务。

**请求参数**：无

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "name": "每日备份",
      "description": "每天凌晨3点执行备份",
      "group_id": 1,
      "spec": "0 0 3 * * *",
      "type": "shell",
      "target": "backup_script.sh",
      "args": ["arg1", "arg2"],
      "timeout": 300,
      "retry_times": 3,
      "retry_interval": 60,
      "status": 1,
      "last_status": 0,
      "last_exec_time": "2023-05-01T03:00:00Z",
      "created_at": "2023-01-01T00:00:00Z",
      "updated_at": "2023-01-01T00:00:00Z"
    }
  ]
}
```

### 2. 获取任务详情

**接口路径**：`GET /api/cron/tasks/:id`

**功能描述**：根据ID获取特定任务的详细信息。

**请求参数**：
- `id`：任务ID（路径参数）

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "name": "每日备份",
    "description": "每天凌晨3点执行备份",
    "group_id": 1,
    "spec": "0 0 3 * * *",
    "type": "shell",
    "target": "backup_script.sh",
    "args": ["arg1", "arg2"],
    "timeout": 300,
    "retry_times": 3,
    "retry_interval": 60,
    "status": 1,
    "last_status": 0,
    "last_exec_time": "2023-05-01T03:00:00Z",
    "created_at": "2023-01-01T00:00:00Z",
    "updated_at": "2023-01-01T00:00:00Z"
  }
}
```

### 3. 添加任务

**接口路径**：`POST /api/cron/tasks`

**功能描述**：添加一个新的定时任务。

**请求参数**：
```json
{
  "name": "任务名称",
  "description": "任务描述",
  "group_id": 1,
  "spec": "0 0 * * * *",
  "type": "function",
  "target": "函数名",
  "args": ["参数1", "参数2"],
  "timeout": 30,
  "retry_times": 3,
  "retry_interval": 10,
  "status": 1
}
```

**参数说明**：
- `name`：任务名称（必填）
- `description`：任务描述
- `group_id`：任务组ID
- `spec`：cron表达式，定义任务执行时间（必填）
- `type`：任务类型，可选值：`function`、`shell`、`tmux_command`、`tmux_raw_command`（必填）
- `target`：目标函数名或脚本路径（对于`function`和`shell`类型必填）
- `args`：参数列表
- `timeout`：超时时间（秒）
- `retry_times`：重试次数
- `retry_interval`：重试间隔（秒）
- `status`：状态，0-禁用，1-启用

**响应示例**：
```json
{
  "code": 200,
  "msg": "添加任务成功",
  "data": {
    "id": 2
  }
}
```

### 4. 更新任务

**接口路径**：`PUT /api/cron/tasks/:id`

**功能描述**：更新指定ID的任务。

**请求参数**：
- `id`：任务ID（路径参数）
- 请求体同添加任务接口

**响应示例**：
```json
{
  "code": 200,
  "msg": "更新任务成功",
  "data": null
}
```

### 5. 删除任务

**接口路径**：`DELETE /api/cron/tasks/:id`

**功能描述**：删除指定ID的任务。

**请求参数**：
- `id`：任务ID（路径参数）

**响应示例**：
```json
{
  "code": 200,
  "msg": "删除任务成功",
  "data": null
}
```

### 6. 执行任务

**接口路径**：`POST /api/cron/tasks/:id/execute`

**功能描述**：立即执行指定ID的任务。

**请求参数**：
- `id`：任务ID（路径参数）

**响应示例**：
```json
{
  "code": 200,
  "msg": "任务执行成功",
  "data": {
    "output": "执行结果输出"
  }
}
```

### 7. 获取任务日志

**接口路径**：`GET /api/cron/tasks/:id/logs`

**功能描述**：获取指定任务的执行日志。

**请求参数**：
- `id`：任务ID（路径参数）
- `page`：页码（查询参数，默认1）
- `page_size`：每页数量（查询参数，默认20）

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 100,
    "page": 1,
    "page_size": 20,
    "logs": [
      {
        "id": 1,
        "task_id": 1,
        "status": 0,
        "output": "执行结果输出",
        "error": "",
        "exec_time": "2023-05-01T03:00:00Z",
        "duration": 5.2
      }
    ]
  }
}
```

### 8. 获取所有内置函数

**接口路径**：`GET /api/cron/functions`

**功能描述**：获取系统中所有可用的内置函数。

**请求参数**：无

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "restart_server": {
      "name": "restart_server",
      "description": "重启服务器",
      "params": [
        {
          "name": "server_id",
          "type": "string",
          "description": "服务器ID"
        }
      ]
    }
  }
}
```

## 二、tmux集成接口

### 1. 获取tmux会话列表

**接口路径**：`GET /api/cron/tmux/sessions`

**功能描述**：获取所有可用的tmux会话列表。

**请求参数**：无

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "name": "dstserver_MyCluster_Master",
      "state": "running"
    },
    {
      "name": "dstserver_MyCluster_Caves",
      "state": "attached"
    }
  ]
}
```

### 2. 获取可用命令列表

**接口路径**：`GET /api/cron/tmux/commands`

**功能描述**：获取所有可用的预定义命令。

**请求参数**：
- `category`：命令类别（查询参数，可选）

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": "save_world",
      "name": "保存世界",
      "description": "保存游戏世界",
      "category": "world",
      "script": "c_save()",
      "needs_params": false,
      "param_desc": ""
    },
    {
      "id": "announce",
      "name": "公告",
      "description": "发送游戏公告",
      "category": "chat",
      "script": "c_announce(\"%s\")",
      "needs_params": true,
      "param_desc": "公告内容"
    }
  ]
}
```

### 3. 测试tmux命令

**接口路径**：`POST /api/cron/tmux/test-command`

**功能描述**：测试执行tmux模块化命令。

**请求参数**：
```json
{
  "session_name": "dstserver_MyCluster_Master",
  "command_id": "save_world",
  "params": ["参数1", "参数2"]
}
```

**响应示例**：
```json
{
  "code": 200,
  "msg": "命令发送成功",
  "data": {
    "session_name": "dstserver_MyCluster_Master",
    "command_id": "save_world",
    "command_name": "保存世界",
    "script": "c_save()"
  }
}
```

### 4. 测试tmux原始命令

**接口路径**：`POST /api/cron/tmux/test-raw-command`

**功能描述**：测试执行tmux原始命令。

**请求参数**：
```json
{
  "session_name": "dstserver_MyCluster_Master",
  "command": "c_announce(\"测试公告\")"
}
```

**响应示例**：
```json
{
  "code": 200,
  "msg": "命令发送成功",
  "data": {
    "session_name": "dstserver_MyCluster_Master",
    "command": "c_announce(\"测试公告\")"
  }
}
```

### 5. 获取tmux任务详情

**接口路径**：`GET /api/cron/tmux-tasks/:task_id`

**功能描述**：根据任务ID获取tmux任务的详细信息。

**请求参数**：
- `task_id`：任务ID（路径参数）

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "task_id": 2,
    "session_name": "dstserver_MyCluster_Master",
    "command_id": "save_world",
    "raw_command": "",
    "command_params": ["参数1", "参数2"],
    "created_at": "2023-05-01T00:00:00Z",
    "updated_at": "2023-05-01T00:00:00Z"
  }
}
```

## 三、tmux任务创建示例

### 1. 创建tmux_command类型任务

**接口路径**：`POST /api/cron/tasks`

**请求示例**：
```json
{
  "name": "定时保存世界",
  "description": "每小时整点保存游戏世界",
  "group_id": 2,
  "spec": "0 0 * * * *",
  "type": "tmux_command",
  "session_name": "dstserver_MyCluster_Master",
  "command_id": "save_world",
  "command_params": [],
  "timeout": 30,
  "retry_times": 3,
  "retry_interval": 10,
  "status": 1
}
```

### 2. 创建tmux_raw_command类型任务

**接口路径**：`POST /api/cron/tasks`

**请求示例**：
```json
{
  "name": "定时公告",
  "description": "每天早上8点发送公告",
  "group_id": 2,
  "spec": "0 0 8 * * *",
  "type": "tmux_raw_command",
  "session_name": "dstserver_MyCluster_Master",
  "raw_command": "c_announce(\"早上好，欢迎来到服务器！\")",
  "timeout": 30,
  "retry_times": 3,
  "retry_interval": 10,
  "status": 1
}
```

## 四、任务组管理接口

### 1. 获取所有任务组

**接口路径**：`GET /api/cron/groups`

**功能描述**：获取所有任务组。

**请求参数**：无

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "name": "系统任务",
      "description": "系统相关的任务",
      "created_at": "2023-01-01T00:00:00Z",
      "updated_at": "2023-01-01T00:00:00Z"
    },
    {
      "id": 2,
      "name": "游戏任务",
      "description": "游戏相关的任务",
      "created_at": "2023-01-01T00:00:00Z",
      "updated_at": "2023-01-01T00:00:00Z"
    }
  ]
}
```

### 2. 添加任务组

**接口路径**：`POST /api/cron/groups`

**功能描述**：添加一个新的任务组。

**请求参数**：
```json
{
  "name": "新任务组",
  "description": "新任务组描述"
}
```

**响应示例**：
```json
{
  "code": 200,
  "msg": "添加任务组成功",
  "data": {
    "id": 3
  }
}
```

### 3. 更新任务组

**接口路径**：`PUT /api/cron/groups/:id`

**功能描述**：更新指定ID的任务组。

**请求参数**：
- `id`：任务组ID（路径参数）
```json
{
  "name": "更新后的任务组名",
  "description": "更新后的任务组描述"
}
```

**响应示例**：
```json
{
  "code": 200,
  "msg": "更新任务组成功",
  "data": null
}
```

### 4. 删除任务组

**接口路径**：`DELETE /api/cron/groups/:id`

**功能描述**：删除指定ID的任务组。

**请求参数**：
- `id`：任务组ID（路径参数）

**响应示例**：
```json
{
  "code": 200,
  "msg": "删除任务组成功",
  "data": null
}
```

## 五、任务导入导出接口

### 1. 导出任务

**接口路径**：`GET /api/cron/export`

**功能描述**：导出所有任务为JSON文件。

**请求参数**：无

**响应**：文件下载（application/json）

### 2. 导入任务

**接口路径**：`POST /api/cron/import`

**功能描述**：从JSON文件导入任务。

**请求参数**：
- `file`：JSON文件（multipart/form-data）

**响应示例**：
```json
{
  "code": 200,
  "msg": "导入任务成功",
  "data": {
    "imported": 5,
    "failed": 0
  }
}
```

## 六、任务统计接口

### 1. 获取任务统计数据

**接口路径**：`GET /api/cron/stats`

**功能描述**：获取任务执行统计数据。

**请求参数**：
- `start_date`：开始日期（查询参数，格式：YYYY-MM-DD）
- `end_date`：结束日期（查询参数，格式：YYYY-MM-DD）

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total_tasks": 10,
    "active_tasks": 8,
    "executions": 1000,
    "success_rate": 98.5,
    "daily_stats": [
      {
        "date": "2023-05-01",
        "executions": 100,
        "success": 98,
        "failed": 2
      }
    ]
  }
}
```

## 七、cron表达式说明

cron表达式用于定义任务的执行时间，格式为：`秒 分 时 日 月 周`

- **秒**：0-59
- **分**：0-59
- **时**：0-23
- **日**：1-31
- **月**：1-12
- **周**：0-6（0表示周日）

**特殊字符**：
- `*`：表示所有可能的值
- `/`：表示增量，如 `0/15` 表示从0开始，每15个单位执行一次
- `-`：表示范围，如 `10-12` 表示10、11、12
- `,`：表示列表，如 `1,3,5` 表示1、3、5

**示例**：
- `0 0 * * * *`：每小时整点执行
- `0 0 0 * * *`：每天凌晨0点执行
- `0 0 8-18 * * 1-5`：工作日（周一至周五）上午8点至下午6点整点执行
- `0 0/30 8-18 * * *`：每天上午8点至下午6点，每30分钟执行一次
